# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks

# revs are frozen to commit SHAs (tags are mutable); update with
# 'pre-commit autoupdate --freeze'

exclude: '^\.asv/results/.+\.json$'
default_stages: [pre-commit]
default_install_hook_types: [pre-commit, commit-msg, pre-merge-commit]

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 3e8a8703264a2f4a69428a0aa4dcb512790b2c8c # frozen: v6.0.0
    hooks:
      - id: check-added-large-files
      - id: check-ast
      - id: check-case-conflict
      - id: check-docstring-first
      - id: check-executables-have-shebangs
      - id: check-illegal-windows-names
      - id: check-merge-conflict
      - id: check-shebang-scripts-are-executable
      - id: check-symlinks
      - id: check-toml
      - id: check-yaml
      - id: destroyed-symlinks
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: forbid-submodules
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: name-tests-test
        args: [--pytest-test-first]
        exclude: tests/(util\.py|_.+\.py)$
      - id: trailing-whitespace
        exclude: '^renv/activate\.R$' # because renv edits it automatically, with trailing whitespace
      - id: no-commit-to-branch
  - repo: https://github.com/sbrunner/hooks
    rev: 60041b789c098988bd01ddff70e7e3a31c1700f1  # frozen: 1.8.0
    hooks:
      - id: copyright
      - id: copyright-required
        files: \.(py|rst)$
        # generated headerless file; untracked, so it only reaches hooks via
        # an explicit `pre-commit run --files`
        exclude: src/bartz/_version\.py$
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: aab412d509121cb5f7533134b7e67f9fab59c682  # frozen: v0.16.4
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
  - repo: https://github.com/DanielNoord/pydocstringformatter
    rev: b658f517409a0864b20fa9d9aa3d178a37fb7f22  # frozen: v1.0.0
    hooks:
      - id: pydocstringformatter
        # config in [tool.pydocstringformatter] (pyproject.toml); the hook
        # already passes --write --quiet. Scoped to the library, whose numpy
        # attribute-docstring conventions this enforces; drop `files` to widen.
        files: ^src/bartz/.*\.py$
  - repo: https://github.com/jsh9/pydoclint
    rev: cf8f4d0b81f933ebc5414acadcf7acdab705baa1  # frozen: 0.9.1
    hooks:
      - id: pydoclint
  - repo: https://github.com/henryiii/validate-pyproject-schema-store
    rev: a011ac2729a668fd92c470a44476f340ad234404  # frozen: 2026.08.15
    hooks:
      - id: validate-pyproject
  - repo: https://github.com/jorisroovers/gitlint
    rev: acc9d9de6369b76d22cb4167029d2035e8730b98 # frozen: v0.19.1
    hooks:
      - id: gitlint # this is already defined to run in stage commit-msg
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: b84c4a0a90bd58c7d82ae8f0d4d5e77b1ddd3ce6  # frozen: 0.12.5
    hooks:
      - id: uv-lock
  - repo: https://github.com/rhysd/actionlint
    rev: 914e7df21a07ef503a81201c76d2b11c789d3fca # frozen: v1.7.12
    hooks:
      - id: actionlint
  - repo: local
    hooks:
      - id: lint-merged-tree
        name: full lint on the merged tree
        # Runs only at merge time (not on ordinary commits). Gates clean
        # auto-merges, whose merge commit git writes without invoking the
        # `pre-commit` stage: catches a merged tree that fails the hooks even
        # though each branch passed in isolation (e.g. a stale WORKAROUND or a
        # hook config changed on one side). `make lint` is `pre-commit run
        # --all-files`; nesting it here is safe (this hook is not in the
        # pre-commit stage, so --all-files does not re-trigger it).
        entry: make lint
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-merge-commit]
      - id: check-workarounds
        name: check stale WORKAROUND markers
        entry: uv run --dev python config/check_workarounds.py
        language: system
        pass_filenames: false
        always_run: true
      - id: check-path-headers
        name: check the `bartz/<path>` comment matches each file's location
        entry: uv run --dev python config/check_path_headers.py
        language: system
        # self-scopes to files carrying the bartz license header; `text` just
        # keeps binaries (e.g. .inv) out of the argument list.
        types: [text]
      - id: vendor-latest-bartz
        name: vendor latest_bartz (for ty)
        # ty checks files under benchmarks/ that import the vendored latest
        # bartz; refresh it first, else those imports do not resolve (e.g. on
        # CI) or resolve against a stale copy. Runs before ty by declaration
        # order, on the same python trigger.
        entry: uv run --dev python benchmarks/_vendor_latest_bartz.py
        language: system
        pass_filenames: false
        types: [python]
      - id: ty
        name: ty check
        # there is no official ty pre-commit hook (and an isolated hook env
        # would not see the project venv anyway), so run the dev-pinned ty.
        # pre-commit feeds it the tracked python files: only the staged ones on
        # a commit (fast incremental check), and all of them under `--all-files`
        # — the comprehensive gate run by `make lint` and at merge time.
        entry: uv run --dev ty check --
        language: system
        types: [python]
      - id: check-returns-docstring
        name: forbid multi-line non-list `Returns` docstring sections
        # Matches a numpy-style `Returns` block whose first two non-blank lines
        # are both at the section's base indent: i.e., multi-line prose. List-
        # form (description on a more-indented continuation line) is skipped via
        # `(?P=i)\S`; the trailing lookahead skips the next section's header.
        language: pygrep
        entry: '^(?P<i>[ \t]*)Returns[ \t]*\n(?P=i)-+[ \t]*\n(?P=i)(?!""")\S[^\n]*\n(?:[ \t]*\n)*(?P=i)(?!""")(?!\w+[ \t]*\n(?P=i)-+[ \t]*$)\S'
        args: [--multiline]
        types: [python]
      - id: forbid-napoleon-type-colon
        name: forbid stray colon in first line of attribute docstrings
        # A lone colon in the first line of an attribute docstring makes sphinx
        # napoleon split it into a `:type:` field, leaking a stray "Type: <text
        # before the colon>" into the html. Napoleon only inspects the docstring's
        # first line, so the match runs from the opening `"""` to the first newline
        # or the closing `"""` (whichever comes first), one-line and multi-line
        # docstrings alike. Matches an attribute assignment or annotation line (one
        # not ending in `:`, i.e. not a def/class header, possibly just a bare `)`
        # closing a multi-line assignment) followed by a same-indent
        # `"""` whose first line holds a lone `:` (not `::`) outside any ``code``
        # span or :role:`target`, with real text after it (a colon at end of line
        # is harmless: napoleon then keeps it in the description). Reword to drop
        # the colon.
        language: pygrep
        entry: '^(?P<i>[ \t]+)\S(?:[^\n]*[^\n:\s])?\n(?P=i)"""(?:``[^`]*``|:[A-Za-z0-9_+.:-]+:`[^`\n]+`|`[^`\n]+`|(?!""")[^:`\n])*:(?!:)(?![A-Za-z0-9_+.:-]+:`)[ \t]*(?!""")\S'
        args: [--multiline]
        types: [python]
      - id: forbid-lam-lamda
        name: forbid `lam`/`lamda` identifier (use `lambda_` per PEP 8)
        language: pygrep
        entry: '\blam(da)?\b'
        types: [python]
      - id: forbid-jaxext-qualified-call
        name: forbid `_jaxext.foo(...)` (use `from bartz._jaxext import foo`)
        language: pygrep
        # lookbehind `(?<!\.)` allows module paths like `bartz._jaxext.foo`;
        # the trailing `\(` limits matches to function calls so docstrings that
        # mention `_jaxext.foo` are not flagged.
        entry: '(?<!\.)\b_jaxext\.\w+\s*\('
        types: [python]
      - id: forbid-jax-vmap-jit-qualified-call
        name: forbid qualified `jax.vmap`/`jax.jit` (use `from jax import vmap, jit`)
        language: pygrep
        # Matches the qualified name as a word (`\b`) so call sites (`jax.jit(`),
        # bare decorators (`@jax.jit`), and references (`partial(jax.jit, ...)`)
        # are all caught. The `(?!`)` negative lookahead skips docstring/comment
        # mentions, which are wrapped in backticks (`` `jax.jit` ``).
        entry: '\bjax\.(vmap|jit)\b(?!`)'
        types: [python]
      - id: forbid-jax-random-qualified-call
        name: forbid `jax.random.foo(...)` (use `from jax import random`)
        language: pygrep
        # the trailing `\(` limits matches to function calls so docstrings that
        # mention `jax.random` are not flagged.
        entry: '\bjax\.random\.\w+\s*\('
        types: [python]
      - id: forbid-mcmc-module-absolute-import
        name: forbid `from bartz import mcmcstep/loop (use `from bartz.mcmcstep import foo`)
        language: pygrep
        # Forbids `from bartz import ...mcmcstep|mcmcloop...` and `import
        # bartz.mcmcstep|mcmcloop`, which enable the `mcmcstep.foo`/`mcmcloop.foo`
        # qualified-access antipattern. `from bartz.mcmcstep import foo` (and
        # any submodule path like `bartz.mcmcstep._state`) is still allowed.
        # Excluded: `src/bartz/__init__.py` (legitimate submodule re-export
        # in the package's own init) and `benchmarks/` (deliberately probes
        # API surface across bartz versions via module-qualified access).
        exclude: '^(src/bartz/__init__\.py|benchmarks/)'
        entry: '^\s*(from\s+bartz\s+import\s+[^\n]*\b(mcmcstep|mcmcloop)\b|import\s+bartz\.(mcmcstep|mcmcloop)\b)'
        types: [python]
      - id: forbid-dunder-all
        name: forbid setting `__all__` in modules
        language: pygrep
        # matches any module-level `__all__` statement (plain/annotated/augmented
        # assignments, `.append`/`.extend`, `del`, etc.). The `^` anchor restricts
        # to column 0, so references inside indented code or docstrings are ignored.
        entry: '^__all__\b'
        types: [python]
      - id: forbid-bare-array-annotation
        name: forbid bare array annotations (wrap in a jaxtyping shape hint)
        language: pygrep
        # Catches an array type used directly as an annotation (`x: Array`,
        # `-> np.ndarray`, `Array | None`) instead of wrapped in a jaxtyping
        # hint (`Float32[Array, 'n p']`). The name must follow `: `, `-> `, or
        # `| ` at subscript depth 0: the bracket-group prefix `(?:[^][#]*\[
        # [^][#]*\])*` keeps wrapped uses and arrays nested in other generics
        # (`PyTree[Array]`, `Callable[..., np.ndarray]`) exempt, and excluding
        # `#` skips comments. The trailing lookahead requires an annotation
        # terminator, skipping docstring prose ("Array of samples"); the
        # `^(?!\w+ =)` guard skips alias definitions (`DummyArray = Array |
        # ...`). Relies on ruff-format spacing.
        entry: '^(?!\w+ =)(?:[^][#]*\[[^][#]*\])*[^][#]*?[:>|] (?:(?:np|numpy|jnp|jax)\.)?(?:(?:Jax)?ArrayLike|Array|NDArray|ndarray|_LazyArray|DummyArray)\b(?=\s*(?:[,:=|)#]|$))'
        types: [python]
